/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // add other settings here if needed, e.g.:
  // images: { domains: ['example.com'] }
};

export default nextConfig;
